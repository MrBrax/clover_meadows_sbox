@using Clover.Components
@using Sandbox;
@using Sandbox.UI;
@inherits Panel
@namespace Clover.Ui

<root>
	@if ( Tool != null )
	{
		<img src=@Tool.ItemData.GetIcon() />
	}
	else
	{
		@Slot
	}
</root>
